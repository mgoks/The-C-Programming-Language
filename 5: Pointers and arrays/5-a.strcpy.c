/* From page 106:
This idioms should be mastered, because you will see it frequently
in C programs. */

// strcpy: copy t to s; pointer version 3
void strcpy(char *s, char *t)
{
    while (*s++ = *t++)
        ;
}